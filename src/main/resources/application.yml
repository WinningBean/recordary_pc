spring:

  profiles:
    active: local

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/recordary?serverTimezone=UTC
    username: root
    password: 111111

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: false

server:
  error:
    whitelabel:
      enabled: false

  port: 8888